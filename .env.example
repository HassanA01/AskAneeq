# Environment: production or development (affects logging format, CORS strictness)
# NODE_ENV=production

# Server port (default: 8000)
PORT=8000

# Public server URL (used for display in startup logs)
# Set this to your deployed URL or ngrok tunnel URL
SERVER_URL=https://your-domain.com

# Log level: fatal, error, warn, info, debug, trace (default: info in production, debug in development)
LOG_LEVEL=info

# Additional CORS origins (comma-separated, beyond the built-in ChatGPT domains)
# CORS_ORIGINS=https://my-other-domain.com,https://staging.example.com

# Rate limit: max requests per 15-minute window per IP on /mcp (default: 100)
# RATE_LIMIT_MAX=100

# Admin dashboard — required to access /admin API routes
# Generate a token with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ADMIN_TOKEN=your-secret-token-here

# SQLite analytics database path (default: ./analytics.db)
# When running via docker-compose, set this to /app/data/analytics.db
# and ensure the /app/data directory is mounted as a persistent volume.
# ANALYTICS_DB_PATH=/app/data/analytics.db

# Calendly booking link for get_availability tool
# CALENDLY_URL=https://calendly.com/your-handle
