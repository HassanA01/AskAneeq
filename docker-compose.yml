services:
  server:
    build: .
    ports:
      - "8000:8000"
    env_file: .env
    volumes:
      - analytics-data:/app/data
    environment:
      ANALYTICS_DB_PATH: /app/data/analytics.db

volumes:
  analytics-data:
